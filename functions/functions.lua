function CustomAlert(xPlayer, zone, drug)
  -- Lav din egene alert her
  -- local message = "Der er en der sælger stoffer i " .. zone .. "!"
  -- exports['visualz_opkaldsliste']:AddCall(nil, message, "police", xPlayer.getCoords(true))
end
